# Documentation: https://direnv.net/man/direnv-stdlib.1.html
source_up_if_exists
dotenv_if_exists
dotenv_if_exists .env.local

export EXTERNAL_HOST=${TUNNEL_USER}-shop.${TUNNEL_DOMAIN}
export VDS_HOST=${TUNNEL_USER}-vds.${TUNNEL_DOMAIN}

use flake
