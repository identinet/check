# Documentation: https://direnv.net/man/direnv-stdlib.1.html
source_up_if_exists
dotenv_if_exists
if [ "${USE_TUNNEL:-}" = "1" ]; then
  dotenv_if_exists .env.tunnel
fi
dotenv_if_exists .env.local

if has nix; then
  use flake
fi
